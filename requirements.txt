# Core dependencies for this repo's own code (value_selection/, steering/
# bridge modules, aggregation/, eval/). Does NOT include ConVA or
# representation-engineering — those are fetched by scripts/setup_dependencies.*
# into third_party/, not pip-installed here (see steering/README.md).

torch>=2.1
transformers>=4.40
scikit-learn>=1.3
scipy>=1.11
numpy>=1.24
pandas>=2.0
tqdm>=4.66
openai>=1.0
sentence-splitter>=1.4  # eval/eval_overton.py
